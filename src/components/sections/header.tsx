import React from "react";
import Navbar from "./navbar";

const Header = () => (
  <header className="py-4 lg:py-5 sticky top-0 bg-white dark:bg-primary z-50 border-b dark:border-gray-600">
    <Navbar />
  </header>
);

export default Header;
